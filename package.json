{
  "name": "fanshen-marketing-site",
  "private": true,
  "type": "module",
  "description": "宣传站静态页 + 内嵌 demo。构建：上级 frontend 需 npm ci；npm run build 生成 demo/。GitHub：推 main 触发 .github/workflows/deploy-marketing-site.yml；本地根路径用默认 base /demo/，CI 自动按仓库名设 VITE_PAGES_BASE。",
  "scripts": {
    "build": "vite build --config ./vite.config.ts --mode marketing",
    "start": "serve . -l 5175 -c serve.json",
    "preview": "npm run build && npm run start"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "serve": "^14.2.4",
    "tailwindcss": "^3.3.6",
    "vite": "^5.0.8"
  }
}
